name: xai_project
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  # Python
  - python=3.10
  
  # PyTorch ecosystem (dejar que conda resuelva versiones compatibles)
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8
  
  # NumPy (CRÍTICO: fijar a 1.x para compatibilidad)
  - numpy<2.0
  
  # Otras dependencias (versiones flexibles)
  - matplotlib
  - seaborn
  - pandas
  - scikit-learn
  - opencv
  - jupyter
  - notebook
  - ipykernel
  - tqdm
  - pillow
  
  # Pip packages
  - pip
  - pip:
      - captum
      - pytorch-gradcam
      - shap
      - kaggle
